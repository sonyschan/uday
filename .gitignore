.DS_Store
.vercel/
# throwaway screenshot / measurement harnesses
_shot.html
_frame.html
_og.html
_fonts.html

# secrets
.env
__pycache__/

# editor droppings — a .env swapfile carries the key
.env*
*.swp

# foundry build artifacts (forge create run from repo root)
out/
cache/

# local-only: strategy memory and owner-keystore scripts (kept off the public
# repo). build_gift_root.py is COMMITTED since the GHA gift automation — its
# formula was already public through every data/gift/*.json anyway.
CLAUDE.md
tools/post-gift.sh
tools/deploy-community.sh
tools/deploy-gift.sh
tools/deploy-dayexit.sh
.playwright-mcp/

# the daily X card is regenerated every run; it is output, not source
data/x-card.png

# local test route for /c/<slug> (Vercel rewrites this in production)
/c/

# the /api functions need these at build time on Vercel, never in the repo
node_modules/
